import { stylesMeta } from "@/lib/styles/meta-registry";

/**
 * Public catalog facts must come from the catalog itself so marketing copy,
 * structured data, and machine-readable discovery files cannot drift.
 */
export const CURATED_STYLE_COUNT = stylesMeta.length;
