# StyleKit 收费验证：待用户统一确认的决策

更新日期：2026-07-11（Asia/Shanghai）

这份文档只收集必须由产品所有者、法律/税务责任人或生产环境所有者决定的事项。仓库内能够安全完成的代码、测试、隐私说明、退出机制和只读核查已先行处理。请直接在每个“你的回答”下填写；不确定时可以写“保持关闭”。

## 一、当前不可争议的事实

- 现有首页、预览卡片、Showcase 和 135 个风格没有被本次收费验证改动。
- 首个 Pack 为 `corporate-clean-saas@0.1.0`，目前仍是 Preview，不是公开可售产品。
- 当前 Offer v2 的冻结价格为首发 CNY 29 与标准 CNY 49；两组除价格外必须完全相同。旧 Offer v1 的 CNY 199/399 仅保留为历史证据，不再用于新实验。
- Offer v2 SHA-256 为 `sha256:99ea80f21a0429ab6443cfbf6b075a40697fa1067167a1afb03a5a769ce4fd19`。
- 当前许可状态为 `draft_requires_final_review`，`publicSaleAuthorized=false`，因此 E2 已验证价格接受和真实销售保持锁定。
- 实验窗口暂定 2026-08-01 00:00 UTC 至 2026-08-31 23:59:59 UTC。
- 当前真实样本为 0/200 qualified visitors、0/20 qualified interviews、0 笔购买，结论只能是 `inconclusive_sample`。
- 最新 service-role 只读检查已能访问三张远端验证表及退出字段；但 RLS/revoke、RPC 权限和迁移校验和仍不能由当前只读探针完整证明，正式采集前仍需数据库负责人核验。
- 本地没有 Supabase CLI/link；Vercel CLI 已登录且账号中存在名为 `stylekit` 的项目，但当前目录未正式 link。
- 当前生产站的隐藏验证路由为 404，尚未部署，也没有在生产采集。
- `PRODUCT_VALIDATION_HMAC_SECRET` 尚未配置；没有已确认的 checkout/订金渠道。
- 本地敏感环境文件权限已收紧为仅当前用户可读写；没有读取或回显任何 secret。

## 二、商业许可与公开销售

### 2.1 许可草案终审

请选择：批准现有草案 / 提出修改 / 继续保持草案。

你的回答：

> 

需要修改的具体条款：

> 

最终法律审核人或责任人：

> 

### 2.2 公开销售授权

是否明确允许将 `publicSaleAuthorized` 改为 `true`？“允许私下访谈/展示”不等于“允许公开销售”。

你的回答：允许 / 不允许 / 仅允许私下验证

> 

## 三、冻结 Offer 与价格

### 3.1 价格实验

已确认采用标准 Pack 的 CNY 29 与 CNY 49 纯价格实验。

你的回答：已确认 CNY 29 / CNY 49；如需再次修改请填写

> 

### 3.2 交付边界

请逐项确认或修改：

- 最多 5 名贡献者：
- 12 个月更新：
- 30 天安装支持：
- 发现同名文件时停止安装：
- 仅保证声明的干净项目兼容范围：
- 预计交付日期或付款后交付时限：

你的补充：

> 

### 3.3 退款条款

当前草案：在声明的干净项目兼容范围内无法完成生产构建，且 StyleKit 经合理支持仍无法解决，可退还 Pack 购买金额；客户自行修改、依赖冲突或超出兼容范围不在保证内。

你的回答：确认 / 修改如下

> 

## 四、支付与强意向证据

当前没有真实 checkout，点击按钮不能计作 E3。请选择下一阶段方式：

- Stripe
- Lemon Squeezy
- Paddle
- 微信/支付宝人工对账的私密付款或订金链接
- 暂时只做访谈和 E0/E1，不收集 E2/E3

你的选择：

> 

订金是否可退、金额、币种、收款主体、退款/拒付负责人：

> 

支付和税务责任人：

> 

## 五、生产基础设施授权

### 5.1 Supabase

是否授权数据库负责人核验远端 migration 014、015 的完整 SQL、RLS/revoke、退出 RPC 与仓库版本一致，并在有差异时应用补充迁移？这是远端写入，当前未执行。

你的回答：授权 / 不授权 / 先确认项目

> 

目标 Supabase 项目名称或负责人：

> 

执行方式：Dashboard SQL / 安装并 link Supabase CLI / 由你执行

> 

### 5.2 Vercel

当前账号中名为 `stylekit` 的项目是否就是目标生产项目？是否允许当前目录 link 到该项目？

你的回答：

> 

### 5.3 环境变量

谁负责生成和配置全新的 `PRODUCT_VALIDATION_HMAC_SECRET`？是否同时配置独立的 `PRODUCT_VALIDATION_INTERNAL_KEY`？建议 Production 与 Preview 使用不同值。

你的回答：

> 

是否需要轮换现有 Supabase service-role/OIDC 等历史凭据：是 / 否 / 由基础设施负责人评估

> 

## 六、实验窗口与招募

是否保持当前窗口：2026-08-01 至 2026-08-31（UTC）？若基础设施未就绪，应创建新窗口/Offer 版本，不能静默挪动已冻结窗口。

你的回答：保持 / 修改为

> 

招募渠道（社区、邮件、社交、私聊、现有用户等）：

> 

20 名访谈对象从哪里获得：

> 

是否提供访谈激励；若有，金额与形式：

> 

数据负责人、停止实验决定人、一次修订批准人：

> 

## 七、隐藏验证页与传播边界

是否批准当前独立验证页视觉？该页不修改旧前端、保持 `noindex`、不进入导航。

你的回答：批准 / 需要修改（请具体说明）

> 

是否允许通过私密直接链接传播：允许 / 不允许

> 

是否允许未来加入公开导航或 SEO：默认不允许；如允许请单独说明

> 

## 八、研究隐私、撤回与保留期

建议规则：

- 联系方式映射：实验结束后 90 天内删除。
- 去标识化研究证据：最长保留 12 个月后复核删除。
- 在线退出：删除该设备的在线事件，参与者保留最小退出标记，避免重新进入样本；关联访谈从评估中排除。
- 付款与税务记录：按适用法律和支付平台要求单独保留。

是否确认：

> 

研究主体名称、隐私联系邮箱或支持渠道：

> 

录音、逐字稿、原始访谈笔记是否采集；保存位置、访问者和删除期限：

> 

## 九、单位经济假设

请确认或修改：

- 最大生产工时：80 小时
- 当前预测生产工时：60 小时
- 外部资产成本上限：CNY 1,500
- 维护者成本：CNY 100/小时
- 支持成本：CNY 60/小时
- 预计退款率：5%
- 税率：6%
- 支付费率：3%
- 每单交付成本：CNY 5
- 每单支持：20 分钟
- 可归因获客成本：CNY 10
- Break-even 上限：25 单

你的修改：

> 

## 十、明确不在本轮自动执行的事项

在收到以上回答之前，系统应继续保持：

- 不公开销售、不收款、不生成订单。
- 不部署或启动正式实验采集。
- 不应用远端数据库迁移、不 link Vercel/Supabase 项目。
- 不修改现有前端设计、预览卡片或 135 个风格。
- 不开发 Pack 2，不把 99–199 单 Pack 或 399–699 All Access 路线混入本轮实验。
