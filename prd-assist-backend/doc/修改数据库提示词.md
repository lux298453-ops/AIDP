清除H2和相关的测试文件（确保不清错），

请帮我把整个项目从 MySQL 切换到 PostgreSQL。

当前技术栈：Spring Boot 3 + Spring Data JPA + Java 17

### 需要修改的内容（请全部覆盖）：

1. **pom.xml 依赖修改**
   - 移除 mysql-connector-j
   - 替换为 PostgreSQL 驱动
   - 确保正确版本

2. **application.yml / application-dev.yml 配置**
   - 修改数据库连接字符串、驱动类、用户名密码
   - 配置 PostgreSQL 专属参数（当前模式、时区等）

3. **实体类（Entity）调整**
   - 把所有 `BIGINT UNSIGNED AUTO_INCREMENT` 改成 PostgreSQL 对应类型
   - JSON 字段处理（PostgreSQL 使用 jsonb）
   - 其他类型兼容性调整（DATETIME → TIMESTAMP 等）

4. **SQL 初始化脚本**
   - 把原来的 `sql.md`（MySQL）完整改成 PostgreSQL 版本
   
- PostgreSQL 18.4                   
   
      - 用户名：postgres<br/>  - 密码：postgres123
   
   - 注意：主键自增、JSON 类型、注释语法、引擎等差异
   
5. **JPA / Hibernate 配置**
   - 修改 dialect 为 PostgreSQLDialect
   - 必要时开启 PostgreSQL 特定功能

6. **其他可能受影响的地方**
   - 任何原生 SQL 查询
   - 数据库相关工具类
   - Docker Compose 配置（如果有）

**请按以下顺序输出修改结果：**
1. 修改后的 `pom.xml` 相关部分
2. 修改后的 `application.yml` 示例
3. 修改后的完整 SQL 建表脚本（PostgreSQL 版本）
4. 实体类需要修改的关键点
5. JPA 配置建议
6. 切换后需要注意的事项（JSONB、序列、自增、大小写敏感等）
7. 相关的文档也要修改，原来内容的打括号表明已经改动做备选

项目使用 JPA 注解开发，请确保兼容性高、改动量最小。
现在开始修改。 